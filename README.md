# aidlc-canary

Disposable canary repository for the Bedrock Application Factory end-to-end test.
The factory creates a branch, writes code with tests, opens a PR, and (after one
human approval) merges it into `master`. Safe to delete.
